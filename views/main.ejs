<!DOCTYPE html>
<html lang="pt-br">

<head>
  <meta charset="utf-8" />
  <title>Não é o Google Meet 2.0</title>
  <link rel="stylesheet" href="style.css" />
  <script src="index.js" charset="utf-8" defer></script>
</head>

<body>
  <div class="header">
    <div class="logo">
      <div class="headerBack">
        <i class="fas fa-angle-left"></i>
      </div>
      <h3>Não é o Google Meet 2.0</h2>
    </div>
  </div>
  <div class="form mgTop">
    <div>Bem-vindo(a) <span id="userName"></span></div>
    <div class="loginContainer">
      <button onclick="getMeetUrl()">Gerar nova sala</button>
      <input readonly placeholder="O código será gerado aqui" type="text" name="meeturl" id="meeturl">
    </div>
  </div>
  <div class="form">
    <div class="loginContainer">
      <input type="text" placeholder="Insira o código da sala" name="goToMeetInput" id="goToMeetInput">
      <button onclick="goToMeet()">Entrar em uma sala</button>
    </div>
  </div>
</body>
<script src="script.js"></script>
<script>
  window.onload = function () {
    if (sessionStorage.getItem('user') == 'user1' || sessionStorage.getItem('user') == 'user2' ||  sessionStorage.getItem('user') == 'user3'){
      console.log(sessionStorage.getItem('user') )
      document.getElementById("userName").innerText = sessionStorage.getItem('user')
    }
    else {
      console.log(window.location.host)
      window.location = 'https://'+window.location.host
    }
  }
</script>
</body>

</html>